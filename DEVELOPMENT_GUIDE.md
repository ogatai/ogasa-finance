# 🛠️ 開発現場の必須コマンド詳細解説集

Web開発の現場において、単に「動く」だけでなく「効率的かつ安全に」開発を進めるための必須コマンドを詳しく解説します。

---

## 1. Git：バージョン管理とチーム連携
チーム開発において、履歴を汚さず、スムーズに作業を切り替えるためのコマンドです。

### 🔹 git stash / git stash pop
- **意味**: 現在の変更内容（書きかけのコード）を一時的に「隠し場所」に保存し、作業ディレクトリをクリーンにします。
- **実行シーン**: 実装の途中で「急ぎのバグ修正」を別のブランチで頼まれた時。
- **詳細**: `stash` を使うことで、コミットするには中途半端なコードを壊さずに退避できます。修正が終わった後、`pop` で元の作業を再開できます。

### 🔹 git commit --amend
- **意味**: 直前のコミット内容を修正し、上書きします。
- **実行シーン**: コミットした直後に「あ、1箇所タイポがあった」「このファイルも含めるべきだった」と気づいた時。
- **詳細**: 新しいコミットログを増やさずに、直前の歴史を綺麗に書き換えることができます。

### 🔹 git clean -fd
- **意味**: Gitの追跡対象外となっているファイル（f）とディレクトリ（d）を強制的に削除します。
- **実行シーン**: 実験的に作ったファイルが溜まった時や、今回のような大規模なディレクトリ整理で「不要なゴミ」を一掃したい時。
- **詳細**: 追跡中の大事なコードは消えませんが、新しく作ったファイルは消えるため、実行前に必ず確認が必要です。

---

## 2. Docker：環境構築とデバッグ
コンテナ環境を常に最新の状態に保ち、不具合の原因を特定するためのコマンドです。

### 🔹 docker compose up -d --build
- **意味**: コンテナをバックグラウンドで起動し、イメージの再ビルドを強制します。
- **実行シーン**: `Dockerfile` や `package.json` を書き換えた直後。
- **詳細**: `--build` をつけないと、古いキャッシュを使って起動してしまい「コードを直したのに反映されない」というトラブルの元になります。

### 🔹 docker compose logs -f [サービス名]
- **意味**: 指定したコンテナのログをリアルタイムで監視（follow）し続けます。
- **実行シーン**: サーバーエラーが発生した時。
- **詳細**: エラーのスタックトレースが流れてくるため、不具合の原因特定には欠かせません。

### 🔹 docker system prune
- **意味**: 使用されていないコンテナ、ネットワーク、イメージをすべて削除します。
- **実行シーン**: PCのストレージが不足してきた時。
- **詳細**: Dockerは放置すると数GBのディスク容量を消費するため、定期的な掃除が必要です。

---

## 3. アプリケーション実行（Node.js / Prisma）
Next.jsなどのモダンな開発環境で、不整合を解消しデータを管理するためのコマンドです。

### 🔹 rm -rf node_modules && npm i
- **意味**: インストール済みのライブラリを一度全て削除し、再度クリーンインストールします。
- **実行シーン**: 「昨日まで動いていたのに、今日突然原因不明のビルドエラーが出る」という時。
- **詳細**: 現場のエンジニアが最初に行う「究極の環境修復術」です。

### 🔹 npx prisma db push
- **意味**: スキーマ定義（`schema.prisma`）をデータベースに直接反映させます。
- **実行シーン**: 開発中、頻繁にテーブル構造を変更する時。
- **詳細**: 履歴を作るマイグレーション（migrate dev）よりも素早く構造を変更できるため、開発スピードを優先する際に使われます。

### 🔹 npx prisma studio
- **意味**: データベースの中身をブラウザ（GUI）で確認できるツールを起動します。
- **実行シーン**: インポートしたCSVデータが正しく保存されているか確認する時。
- **詳細**: SQLを書かずにデータを一覧表示・編集できるため、デバッグ効率が大幅に向上します。

---

> **⚠️ 注意:** ディレクトリ整理や削除コマンドを実行する前には、必ず `git add . && git commit` で現状をバックアップしてください。